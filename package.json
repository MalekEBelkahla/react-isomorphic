{
  "name": "react-opti",
  "version": "0.1.0",
  "description": "Server-side React with prefetched links",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.jsx --debug --extension=.jsx > public/bundle.js -v",
    "watch": "watchify index.jsx --debug --extension=.jsx -o public/bundle.js"
  },
  "author": "Joacim Löwgren",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.12.1",
    "express": "^4.12.2",
    "express-handlebars": "^1.2.2",
    "morgan": "^1.5.2",
    "node-jsx": "^0.12.4",
    "react": "^0.12.0",
    "request": "^2.53.0",
    "serve-static": "^1.9.2"
  },
  "devDependencies": {
    "browserify": "^9.0.3",
    "debug": "^2.1.3",
    "reactify": "^1.1.0",
    "watchify": "^2.4.0"
  },
  "browserify": {
    "transform": [
      "reactify"
    ]
  }
}
